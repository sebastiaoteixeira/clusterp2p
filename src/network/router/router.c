#include "router_table.h"
#include "routing_protocol.h"

// When a message is received, the router will handle it
void router_handler(int socket, void* msg, size_t len)
{
    
}

int main() {
    return 0;
}